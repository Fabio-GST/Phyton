# Elaborar um programa que efetue a leitura de três valores (A, B e C) e apresente como resultado final à
# soma dos quadrados dos três valores lidos

A = float(input("\nA: "))
B = float(input("\nB: "))
C = float(input("\nC: "))

print("A soma dos quadrados de A,B e C é: ", (A**2)+(B**2)+(C**2))